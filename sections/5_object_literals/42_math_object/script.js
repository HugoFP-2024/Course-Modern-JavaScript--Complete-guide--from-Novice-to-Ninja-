// Math object:
// The Math object is a pre-built object native in JS, that has several properties and methods that can help us out.

// With this log in your browser, you can see all the properties and attributes that Math has
console.log(Math);

// You can use Math for all sorts of, well, math-related stuff, like rounding numbers, or generating random numbers, and a lot of other stuff.