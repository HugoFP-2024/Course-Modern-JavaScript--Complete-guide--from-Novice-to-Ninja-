// Return Values:
// Needed for the function to explicitly return a value that can be, for example, stored in a variable.

const calcArea = function (radius) {
    return 3.14 * radius ** 2;
};

let area = calcArea(5);
console.log(area);